# Vercel auth build diagnostic

Temporary CI diagnostic branch used to run the repository's pull-request checks against the current production auth callback implementation. Remove this file after the build issue is identified.
