"use client";
import{useCallback,useEffect,useState}from"react";
type Reply={id:string;intent_id:string|null;contact_id:string|null;from_email:string;subject:string;text_body:string|null;received_at:string;matched_by:"in_reply_to"|"references"|"unmatched";review_status:"unreviewed"|"reviewed"|"not_a_reply";review_note:string|null;contacts:{company_name:string;buyer_name:string|null;email:string|null}|null};
export function OutreachReplyPanel(){const[replies,setReplies]=useState<Reply[]>([]);const[error,setError]=useState("");const[busy,setBusy]=useState<string|null>(null);const load=useCallback(async()=>{const response=await fetch("/api/outreach-replies",{cache:"no-store"});const body=await response.json();if(!response.ok)throw new Error(body.error||"Unable to load replies.");setReplies(body.replies??[]);},[]);useEffect(()=>{load().catch(e=>setError(e instanceof Error?e.message:"Unable to load replies."));},[load]);async function review(id:string,status:"reviewed"|"not_a_reply"){setBusy(id);setError("");try{const response=await fetch("/api/outreach-replies",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({id,status})});const body=await response.json();if(!response.ok)throw new Error(body.error||"Unable to update reply.");await load();}catch(e){setError(e instanceof Error?e.message:"Unable to update reply.");}finally{setBusy(null);}}const unreviewed=replies.filter(r=>r.review_status==="unreviewed");return <div className="panel"><div className="panel-heading"><div><span className="section-label">REPLY INBOX · HUMAN REVIEW</span><h2>{unreviewed.length} unreviewed repl{unreviewed.length===1?"y":"ies"}</h2><p>Replies are captured and correlated to the approved outbound message when headers prove the relationship. No response is sent automatically.</p></div></div>{error&&<div className="alert error">{error}</div>}<div className="activity-list">{replies.map(reply=><div className="match-row" key={reply.id}><div><strong>{reply.contacts?.company_name??reply.from_email}{reply.contacts?.buyer_name?` · ${reply.contacts.buyer_name}`:""}</strong><p>{reply.from_email} · {reply.subject||"(no subject)"}</p><p>{new Date(reply.received_at).toLocaleString()} · {reply.matched_by==="unmatched"?"Unmatched inbound email":`Matched by ${reply.matched_by.replaceAll("_"," ")}`}</p>{reply.text_body&&<p style={{whiteSpace:"pre-wrap"}}>{reply.text_body.slice(0,1200)}</p>}</div><div className="account-controls"><span className={`badge ${reply.review_status==="reviewed"?"valid":reply.review_status==="unreviewed"?"warning":""}`}>{reply.review_status.replaceAll("_"," ")}</span>{reply.review_status==="unreviewed"&&<><button onClick={()=>review(reply.id,"reviewed")} disabled={busy!==null}>{busy===reply.id?"Saving…":"Mark reviewed"}</button><button className="secondary" onClick={()=>review(reply.id,"not_a_reply")} disabled={busy!==null}>Not a reply</button></>}</div></div>)}{!replies.length&&<div className="empty">No inbound replies have been captured yet.</div>}</div></div>}
