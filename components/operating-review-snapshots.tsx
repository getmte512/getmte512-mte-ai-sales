"use client";
import{useState}from"react";import{useRouter}from"next/navigation";import type{ReviewPeriod}from"@/lib/sales-operating-review";
type Snapshot={id:string;period:ReviewPeriod;as_of_date:string;current_start:string;current_end:string;previous_start:string;previous_end:string;payload_hash:string;recorded_at:string};
export function OperatingReviewSnapshots({asOf,snapshots}:{asOf:string;snapshots:Snapshot[]}){const router=useRouter();const[busy,setBusy]=useState<ReviewPeriod|null>(null);const[message,setMessage]=useState("");async function record(period:ReviewPeriod){setBusy(period);setMessage("");try{const response=await fetch("/api/review/snapshots",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({period,as_of:asOf,confirmation:"RECORD_OPERATING_REVIEW_SNAPSHOT"})});const body=await response.json();if(!response.ok)throw new Error(body.error??"Unable to record snapshot.");setMessage(body.snapshot?.already_recorded?`${period} snapshot was already recorded with the same evidence.`:`${period} snapshot recorded and audit evidence created.`);router.refresh()}catch(error){setMessage(error instanceof Error?error.message:"Unable to record snapshot.")}finally{setBusy(null)}}return <section className="panel"><span className="section-label">MILESTONE 18 · IMMUTABLE REVIEW HISTORY</span><h2>Record today’s operating evidence</h2><p>Recording recomputes the review on the server, stores the canonical payload and SHA-256 hash, and creates audit evidence. A recorded period/date cannot be overwritten with different evidence.</p><div className="account-controls"><button className="secondary" disabled={busy!==null} onClick={()=>record("week")}>{busy==="week"?"Recording…":"Record weekly snapshot"}</button><button className="secondary" disabled={busy!==null} onClick={()=>record("month")}>{busy==="month"?"Recording…":"Record monthly snapshot"}</button></div>{message&&<div className="alert">{message}</div>}<div className="activity-list">{snapshots.map(snapshot=><article key={snapshot.id} className="match-row"><div><strong>{snapshot.period==="week"?"Weekly":"Monthly"} · {snapshot.as_of_date}</strong><p>{snapshot.current_start} through {snapshot.current_end} · recorded {new Date(snapshot.recorded_at).toLocaleString()}</p><p>Evidence hash: <code>{snapshot.payload_hash}</code></p></div><span className="badge valid">immutable</span></article>)}{!snapshots.length&&<div className="empty">No operating-review snapshots have been recorded yet.</div>}</div></section>}
