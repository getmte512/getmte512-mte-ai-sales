import{describe,expect,it}from"vitest";import{buildGoalFocus}from"./sales-operating-goal-focus";import type{TargetScore}from"./sales-operating-targets";
const row=(metric:TargetScore["metric"],actual:number,target:number,status:TargetScore["status"]):TargetScore=>({metric,label:metric,actual,target,gap:actual-target,achievementPct:target?Math.round((actual/target)*1000)/10:null,status});
describe("goal focus",()=>{it("ranks larger percentage shortfalls first",()=>{const focus=buildGoalFocus([row("revenue",50,100,"behind"),row("orders",90,100,"behind")]);expect(focus.map(item=>item.metric)).toEqual(["revenue","orders"]);expect(focus[0].priority).toBe(50);expect(focus[1].priority).toBe(10)});it("excludes met and unset targets",()=>{expect(buildGoalFocus([row("revenue",120,100,"met"),{...row("orders",1,0,"unset"),target:null,gap:null}])).toEqual([])});it("maps focus to existing human-controlled workspaces",()=>{const focus=buildGoalFocus([row("outreachDelivered",5,10,"behind"),row("prospectsAccepted",1,4,"behind")]);expect(focus.some(item=>item.href==="/delivery")).toBe(true);expect(focus.some(item=>item.href==="/prospects")).toBe(true)});});
