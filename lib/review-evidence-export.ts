import{createHash}from"node:crypto";
function canonical(value:unknown):string{if(value===null||typeof value!=="object")return JSON.stringify(value);if(Array.isArray(value))return`[${value.map(canonical).join(",")}]`;const record=value as Record<string,unknown>;return`{${Object.keys(record).sort().map(key=>`${JSON.stringify(key)}:${canonical(record[key])}`).join(",")}}`;}
export function buildReviewEvidenceExport(input:{snapshots:unknown[];annotations:unknown[];generatedAt:string}){const evidence={formatVersion:1,generatedAt:input.generatedAt,snapshots:input.snapshots,annotations:input.annotations};const evidenceHash=createHash("sha256").update(canonical({formatVersion:evidence.formatVersion,snapshots:evidence.snapshots,annotations:evidence.annotations})).digest("hex");return{...evidence,manifest:{snapshotCount:input.snapshots.length,annotationCount:input.annotations.length,evidenceHash}};}
