import{describe,expect,it}from"vitest";import{buildOperatingTargetHistory}from"./sales-operating-target-history";import type{OperatingTarget}from"./sales-operating-targets";
const target=(id:string,value:number,setAt:string,effectiveTo:string|null):OperatingTarget=>({id,period:"week",metric:"revenue",targetValue:value,effectiveFrom:"2026-08-18",effectiveTo,note:`target ${value}`,setBy:"12345678-0000-0000-0000-000000000000",setAt});
describe("operating target history",()=>{it("computes prior value and delta in version order",()=>{const rows=buildOperatingTargetHistory([target("new",120,"2026-08-18T02:00:00Z",null),target("old",100,"2026-08-18T01:00:00Z","2026-08-18")]);expect(rows[0]).toMatchObject({id:"new",previousValue:100,delta:20,isActive:true});expect(rows[1]).toMatchObject({id:"old",previousValue:null,delta:null,isActive:false})});it("keeps different period/metric histories independent",()=>{const month={...target("month",300,"2026-08-18T03:00:00Z",null),period:"month" as const};const rows=buildOperatingTargetHistory([target("week",100,"2026-08-18T01:00:00Z",null),month]);expect(rows.every(row=>row.previousValue===null)).toBe(true)});});
