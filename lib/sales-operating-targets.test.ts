import{describe,expect,it}from"vitest";import{buildOperatingTargetScorecard,type OperatingTarget}from"./sales-operating-targets";
const actuals={revenue:1200,orders:12,outreachDelivered:20,buyerReplies:8,tasksCompleted:15,prospectsAccepted:4,samplesDelivered:3,commandCardsCompleted:10};
const target=(metric:OperatingTarget["metric"],value:number):OperatingTarget=>({id:metric,period:"week",metric,targetValue:value,effectiveFrom:"2026-08-18",effectiveTo:null,note:"weekly target",setBy:"admin",setAt:"2026-08-18T00:00:00Z"});
describe("operating target scorecard",()=>{it("marks met and behind targets deterministically",()=>{const rows=buildOperatingTargetScorecard("week",actuals,[target("revenue",1000),target("orders",20)]);expect(rows.find(row=>row.metric==="revenue")).toMatchObject({status:"met",gap:200,achievementPct:120});expect(rows.find(row=>row.metric==="orders")).toMatchObject({status:"behind",gap:-8,achievementPct:60})});it("does not invent achievement percentages for zero or missing targets",()=>{const rows=buildOperatingTargetScorecard("week",actuals,[target("buyerReplies",0)]);expect(rows.find(row=>row.metric==="buyerReplies")?.achievementPct).toBeNull();expect(rows.find(row=>row.metric==="samplesDelivered")).toMatchObject({status:"unset",target:null,gap:null,achievementPct:null})});it("ignores closed target versions",()=>{const closed={...target("revenue",9999),effectiveTo:"2026-08-17"};expect(buildOperatingTargetScorecard("week",actuals,[closed,target("revenue",1000)]).find(row=>row.metric==="revenue")?.target).toBe(1000)});});
