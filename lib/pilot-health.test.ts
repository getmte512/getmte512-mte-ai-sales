import{describe,expect,it}from"vitest";import{summarizePilotHealth}from"./pilot-health";
const ready={allowed:true,reason:"Pilot launch gates are satisfied.",smokeReady:true,smokeFresh:true,missingVerifications:[]};
const blocked={allowed:false,reason:"Record a fresh production smoke test.",smokeReady:true,smokeFresh:false,missingVerifications:[]};
describe("pilot health",()=>{it("is idle with no live pilot accounts",()=>expect(summarizePilotHealth([],ready).status).toBe("idle"));it("is healthy when live accounts retain launch readiness",()=>expect(summarizePilotHealth([{id:"1",contactId:"c",status:"active"}],ready)).toMatchObject({status:"healthy",requiresReview:false,activeCount:1}));it("flags every invited or active account when readiness degrades",()=>expect(summarizePilotHealth([{id:"1",contactId:"c1",status:"active"},{id:"2",contactId:"c2",status:"invited"},{id:"3",contactId:"c3",status:"paused"}],blocked)).toMatchObject({status:"degraded",requiresReview:true,affectedAccountIds:["1","2"],liveCount:2}));});
