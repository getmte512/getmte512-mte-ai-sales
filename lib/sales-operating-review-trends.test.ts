import{describe,expect,it}from"vitest";import{buildHistoricalTrend,type HistoricalSnapshot,type ReviewSnapshotPayload}from"./sales-operating-review-trends";
function payload(period:"week"|"month",revenue:number,orders:number):ReviewSnapshotPayload{return{period,window:{currentStart:"2026-08-01",currentEnd:"2026-08-07",previousStart:"2026-07-25",previousEnd:"2026-07-31",days:period==="week"?7:30},metrics:{revenue:{current:revenue,previous:0,delta:revenue,deltaPct:null},orders:{current:orders,previous:0,delta:orders,deltaPct:null},outreachDelivered:{current:0,previous:0,delta:0,deltaPct:null},buyerReplies:{current:0,previous:0,delta:0,deltaPct:null},tasksCompleted:{current:0,previous:0,delta:0,deltaPct:null},prospectsAccepted:{current:0,previous:0,delta:0,deltaPct:null},samplesDelivered:{current:0,previous:0,delta:0,deltaPct:null},commandCardsCompleted:{current:0,previous:0,delta:0,deltaPct:null}},activity:{commandCardsDismissed:0,commandCardsDeferred:0,replyToDeliveryRatio:{current:null,previous:null}},signals:[]}}
function snap(id:string,period:"week"|"month",date:string,revenue:number,orders:number):HistoricalSnapshot{return{id,period,asOfDate:date,recordedAt:`${date}T12:00:00Z`,payload:payload(period,revenue,orders)}}
describe("historical operating-review trends",()=>{it("compares the two newest immutable snapshots for the requested period",()=>{const result=buildHistoricalTrend([snap("1","week","2026-08-01",100,2),snap("2","week","2026-08-08",150,3),snap("3","month","2026-08-08",999,9)],"week");expect(result?.latestAsOf).toBe("2026-08-08");expect(result?.priorAsOf).toBe("2026-08-01");expect(result?.rows.find(row=>row.key==="revenue")).toMatchObject({latest:150,prior:100,delta:50,deltaPct:50});});it("never invents percentage growth from a zero historical baseline",()=>{const result=buildHistoricalTrend([snap("1","week","2026-08-01",0,0),snap("2","week","2026-08-08",100,2)],"week");expect(result?.rows.find(row=>row.key==="revenue")?.deltaPct).toBeNull();});it("requires two snapshots of the same period",()=>{expect(buildHistoricalTrend([snap("1","week","2026-08-08",100,2)],"week")).toBeNull();});});
