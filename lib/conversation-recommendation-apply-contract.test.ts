import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";
const migration=readFileSync("supabase/migrations/047_apply_conversation_recommendation.sql","utf8");const route=readFileSync("app/api/reply-intelligence/apply/route.ts","utf8");const panel=readFileSync("components/outreach-reply-panel.tsx","utf8");
describe("accepted conversation recommendation pipeline application",()=>{it("requires an accepted recommendation and records apply evidence",()=>{expect(migration).toContain("if v_rec.status <> 'accepted'");expect(migration).toContain("pipeline_applied_by");expect(migration).toContain("pipeline_applied_at");expect(migration).toContain("conversation_recommendation_pipeline_applied");});it("is idempotent and service-role-only",()=>{expect(migration).toContain("if v_rec.pipeline_applied_at is not null then");expect(migration).toContain("'already_applied',true");expect(migration).toContain("to service_role");});it("requires a separate literal apply confirmation",()=>{expect(route).toContain("APPLY_ACCEPTED_RECOMMENDATION_TO_PIPELINE");expect(route).toContain("apply_conversation_recommendation_to_pipeline");expect(panel).toContain("Apply pipeline move");});it("does not gain message-send authority",()=>{expect(route).not.toContain("sendApprovedEmailWithResend");expect(route).not.toContain('/api/delivery-provider');expect(panel).toContain("It will not send the drafted response");expect(panel).toContain("Response draft still requires separate approval");});});
