export function createReorderDraft(input:{buyerName:string|null;companyName:string;orderedOn:string|null;openingOrderValue:number|null}){const greeting=input.buyerName?.trim()?`Hi ${input.buyerName.trim().split(/\s+/)[0]},`:"Hello,";return{subject:`Checking in on ${input.companyName}'s More Than Energy inventory`,body:`${greeting}\n\nI wanted to check in on how your More Than Energy inventory is looking${input.orderedOn?` since your order on ${input.orderedOn}`:""}. If any flavors are running low, I can help review what may be useful for your next order.\n\nThere is no automatic reorder—just reply when you are ready and we will confirm everything with you first.\n\nBest,\nScott\nMore Than Energy`};}
