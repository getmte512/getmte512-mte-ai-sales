import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";
const migration=readFileSync("supabase/migrations/041_prospect_discovery_budget_guard.sql","utf8");const route=readFileSync("app/api/prospect-discovery/search/route.ts","utf8");
describe("prospect discovery budget contract",()=>{it("serializes starts per user before counting usage",()=>{expect(migration).toContain("pg_advisory_xact_lock");expect(migration).toContain("v_searches_today >= p_max_searches_per_day");expect(migration).toContain("v_candidates_today + p_requested_limit > p_max_candidates_per_day");expect(migration).toContain("search cooldown is active");});it("keeps budgeted start RPCs service-role-only",()=>{expect(migration).toContain("start_prospect_discovery_search_budgeted");expect(migration).toContain("start_prospect_discovery_search_for_profile_budgeted");expect(migration).toContain("to service_role");});it("uses budgeted RPCs before invoking external discovery",()=>{const budgeted=route.indexOf("start_prospect_discovery_search_budgeted");const provider=route.indexOf("discoverProspectsWithWebSearch");expect(budgeted).toBeGreaterThan(-1);expect(provider).toBeGreaterThan(budgeted);expect(route).toContain("status:429");});});
