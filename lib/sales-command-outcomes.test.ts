import{describe,expect,it}from"vitest";import{applyCommandDecisions,commandDecisionFingerprint}from"./sales-command-outcomes";import type{CommandCenterItem}from"./sales-command-center";
const item:CommandCenterItem={id:"task:t1",kind:"task",lane:"act_now",title:"Buyer Co",action:"Call buyer",reason:"Task overdue since 2026-08-17",priority:85,href:"/",contactId:"c1",relatedSignals:[],signalCount:1};
describe("sales command outcomes",()=>{it("uses action and reason in the fingerprint so materially changed recommendations reappear",()=>{const changed={...item,reason:"Task due today"};expect(commandDecisionFingerprint(item)).not.toBe(commandDecisionFingerprint(changed));});it("hides completed and dismissed recommendation cards without mutating the source item",()=>{const completed=applyCommandDecisions([item],[{itemFingerprint:commandDecisionFingerprint(item),outcome:"completed"}],"2026-08-18");expect(completed.visible).toHaveLength(0);expect(completed.summary.completed).toBe(1);expect(item.action).toBe("Call buyer");});it("hides active deferrals and restores them when the defer date expires",()=>{const fingerprint=commandDecisionFingerprint(item);expect(applyCommandDecisions([item],[{itemFingerprint:fingerprint,outcome:"deferred",deferUntil:"2026-08-20"}],"2026-08-18").visible).toHaveLength(0);const expired=applyCommandDecisions([item],[{itemFingerprint:fingerprint,outcome:"deferred",deferUntil:"2026-08-18"}],"2026-08-18");expect(expired.visible).toHaveLength(1);expect(expired.summary.expiredDeferrals).toBe(1);});});
