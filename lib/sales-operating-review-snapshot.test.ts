import{describe,expect,it}from"vitest";import{operatingReviewSnapshotHash}from"./sales-operating-review-snapshot";
describe("operating review snapshot hash",()=>{it("is stable across object key order",()=>{const a=operatingReviewSnapshotHash({period:"week",asOf:"2026-08-18",review:{b:2,a:1}});const b=operatingReviewSnapshotHash({review:{a:1,b:2},asOf:"2026-08-18",period:"week"});expect(a).toBe(b);expect(a).toMatch(/^[0-9a-f]{64}$/)});it("changes when period evidence changes",()=>{const a=operatingReviewSnapshotHash({period:"week",asOf:"2026-08-18",review:{revenue:100}});const b=operatingReviewSnapshotHash({period:"week",asOf:"2026-08-18",review:{revenue:101}});expect(a).not.toBe(b)});});
