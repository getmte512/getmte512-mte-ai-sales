import{describe,expect,it}from"vitest";import{buildTargetAttainmentTrend,type TargetAwareSnapshot}from"./sales-operating-target-trends";
const snap=(date:string,met:boolean):TargetAwareSnapshot=>({period:"week",asOfDate:date,targetHash:"a".repeat(64),scorecard:[{metric:"revenue",label:"Revenue",actual:met?120:80,target:100,gap:met?20:-20,achievementPct:met?120:80,status:met?"met":"behind"},{metric:"orders",label:"Orders",actual:10,target:null,gap:null,achievementPct:null,status:"unset"}]});
describe("target attainment trends",()=>{it("compares newest immutable target-aware snapshots",()=>{const trend=buildTargetAttainmentTrend([snap("2026-08-10",false),snap("2026-08-17",true)],"week");expect(trend?.latestAsOf).toBe("2026-08-17");expect(trend?.latest).toMatchObject({configured:1,met:1,attainmentPct:100});expect(trend?.prior).toMatchObject({configured:1,met:0,attainmentPct:0});expect(trend?.rows[0]).toMatchObject({priorStatus:"behind",latestStatus:"met",achievementDeltaPct:40})});it("requires two target-aware snapshots",()=>{expect(buildTargetAttainmentTrend([snap("2026-08-17",true)],"week")).toBeNull();expect(buildTargetAttainmentTrend([{...snap("2026-08-10",false),targetHash:null},snap("2026-08-17",true)],"week")).toBeNull()});});
