import{describe,expect,it}from"vitest";import{evaluatePilotLaunchGate,statusStartsPilot}from"./pilot-launch-gate";
const now=Date.parse("2026-08-18T16:00:00Z");const fresh="2026-08-18T15:00:00Z";
describe("pilot launch gate",()=>{it("fails closed without a ready recorded smoke test",()=>expect(evaluatePilotLaunchGate({latestSmokeStatus:null,latestSmokeCheckedAt:null,verificationKeys:["invitation","approval_flow","backup_restore"]},now).allowed).toBe(false));it("fails closed when a clean smoke test is stale",()=>{const result=evaluatePilotLaunchGate({latestSmokeStatus:"ready",latestSmokeCheckedAt:"2026-08-17T15:59:59Z",verificationKeys:["invitation","approval_flow","backup_restore"]},now);expect(result.allowed).toBe(false);expect(result.smokeFresh).toBe(false)});it("fails closed when a real-world verification is missing",()=>{const result=evaluatePilotLaunchGate({latestSmokeStatus:"ready",latestSmokeCheckedAt:fresh,verificationKeys:["invitation","approval_flow"]},now);expect(result.allowed).toBe(false);expect(result.missingVerifications).toEqual(["backup_restore"])});it("allows launch only when fresh smoke and all verification gates pass",()=>expect(evaluatePilotLaunchGate({latestSmokeStatus:"ready",latestSmokeCheckedAt:fresh,verificationKeys:["invitation","approval_flow","backup_restore"]},now).allowed).toBe(true));it("guards invited and active transitions only",()=>{expect(statusStartsPilot("selected")).toBe(false);expect(statusStartsPilot("paused")).toBe(false);expect(statusStartsPilot("invited")).toBe(true);expect(statusStartsPilot("active")).toBe(true)})});
