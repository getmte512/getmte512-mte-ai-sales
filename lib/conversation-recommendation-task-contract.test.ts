import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";
const migration=readFileSync("supabase/migrations/048_conversation_recommendation_tasks.sql","utf8");const route=readFileSync("app/api/reply-intelligence/task/route.ts","utf8");const panel=readFileSync("components/outreach-reply-panel.tsx","utf8");
describe("accepted conversation recommendation follow-up task",()=>{it("requires accepted recommendation and sales access",()=>{expect(migration).toContain("if v_rec.status <> 'accepted'");expect(migration).toContain("role in ('admin','sales')");expect(migration).toContain("to service_role");});it("is idempotent and audited",()=>{expect(migration).toContain("if v_rec.task_id is not null then");expect(migration).toContain("'already_applied',true");expect(migration).toContain("conversation_recommendation_task_created");});it("requires explicit confirmation and has no send authority",()=>{expect(route).toContain("CREATE_RECOMMENDED_FOLLOW_UP_TASK");expect(route).not.toContain("sendApprovedEmailWithResend");expect(route).not.toContain("delivery-provider");expect(panel).toContain("Create follow-up task");expect(panel).toContain("will not send a message");});});
