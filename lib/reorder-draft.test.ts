import{describe,expect,it}from"vitest";import{createReorderDraft}from"./reorder-draft";
describe("reorder reminder draft",()=>{it("personalizes the buyer and company",()=>{const draft=createReorderDraft({buyerName:"Kathleen Jones",companyName:"The Health Mart",orderedOn:"2026-07-01",openingOrderValue:500});expect(draft.subject).toContain("The Health Mart");expect(draft.body).toContain("Hi Kathleen,");expect(draft.body).toContain("no automatic reorder");});it("uses a safe greeting when buyer is missing",()=>expect(createReorderDraft({buyerName:null,companyName:"Shop",orderedOn:null,openingOrderValue:null}).body).toContain("Hello,"));});
