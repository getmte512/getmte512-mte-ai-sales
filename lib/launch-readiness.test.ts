import{describe,expect,it}from"vitest";import{getLaunchVerificationEvidence,summarizeLaunchReadiness}from"./launch-readiness";
describe("launch readiness",()=>{it("blocks when a required gate fails",()=>expect(summarizeLaunchReadiness([{name:"DB",passed:false,required:true,detail:""}]).decision).toBe("not_ready"));it("allows a pilot with optional work pending",()=>expect(summarizeLaunchReadiness([{name:"DB",passed:true,required:true,detail:""},{name:"Shopify",passed:false,required:false,detail:""}]).decision).toBe("pilot_ready"));it("reports launch ready when all gates pass",()=>expect(summarizeLaunchReadiness([{name:"DB",passed:true,required:true,detail:""}]).decision).toBe("launch_ready"));it("fails closed when real-world verification evidence is absent or invalid",()=>{expect(getLaunchVerificationEvidence({})).toEqual({invitationVerifiedAt:null,approvalFlowVerifiedAt:null,backupRestoreVerifiedAt:null});expect(getLaunchVerificationEvidence({LAUNCH_INVITATION_VERIFIED_AT:"not-a-date"}).invitationVerifiedAt).toBeNull();expect(getLaunchVerificationEvidence({LAUNCH_APPROVAL_FLOW_VERIFIED_AT:"2999-01-01T00:00:00Z"}).approvalFlowVerifiedAt).toBeNull()});it("accepts completed verification timestamps without exposing credentials",()=>{const evidence=getLaunchVerificationEvidence({LAUNCH_INVITATION_VERIFIED_AT:"2026-08-17T12:00:00Z",LAUNCH_APPROVAL_FLOW_VERIFIED_AT:"2026-08-17T12:05:00Z",LAUNCH_BACKUP_RESTORE_VERIFIED_AT:"2026-08-17T12:10:00Z",SUPABASE_SERVICE_ROLE_KEY:"secret"});expect(evidence.invitationVerifiedAt).toBe("2026-08-17T12:00:00.000Z");expect(JSON.stringify(evidence)).not.toContain("secret")})});
