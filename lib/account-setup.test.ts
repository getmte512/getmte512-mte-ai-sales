import{describe,expect,it}from"vitest";import{inviteRedirectUrl,passwordSetupSchema}from"./account-setup";
describe("account setup",()=>{it("requires a strong matching password",()=>{expect(passwordSetupSchema.safeParse({password:"short",confirmPassword:"short"}).success).toBe(false);expect(passwordSetupSchema.safeParse({password:"a-secure-passphrase",confirmPassword:"different-passphrase"}).success).toBe(false)});it("accepts a matching passphrase",()=>expect(passwordSetupSchema.safeParse({password:"a-secure-passphrase",confirmPassword:"a-secure-passphrase"}).success).toBe(true));it("builds the invitation callback on the configured origin",()=>expect(inviteRedirectUrl("http://localhost:3001/api/access","https://crm.getmte.com")).toBe("https://crm.getmte.com/auth/confirm"));});
