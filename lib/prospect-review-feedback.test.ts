import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";
const migration=readFileSync("supabase/migrations/040_prospect_review_feedback.sql","utf8");const route=readFileSync("app/api/prospect-discovery/route.ts","utf8");const panel=readFileSync("components/prospect-discovery-panel.tsx","utf8");
describe("prospect review feedback",()=>{it("stores only allowed review reasons",()=>{for(const reason of ["strong_fit","verified_buyer","strategic_account","wrong_retailer_fit","wrong_role","weak_source","stale_contact","insufficient_evidence","duplicate_existing","other"])expect(migration).toContain(reason);});it("keeps structured feedback in the atomic review transaction",()=>{expect(migration).toContain("v_result := public.review_prospect_discovery_candidate");expect(migration).toContain("set review_reason=p_review_reason");expect(migration).toContain("prospect_discovery_review_reason_recorded");});it("requires a reason at the API boundary and uses the reason-aware RPC",()=>{expect(route).toContain("review_reason:reviewReason");expect(route).toContain("review_prospect_discovery_candidate_with_reason");});it("requires compatible accept or reject reasons before review",()=>{expect(panel).toContain("acceptReasons.has");expect(panel).toContain("rejectReasons.has");expect(panel).toContain("Decision reason");expect(panel).toContain("Every decision requires a structured reason plus a review note");});});
