import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";
const migration=readFileSync("supabase/migrations/048_conversation_response_approval.sql","utf8");const route=readFileSync("app/api/conversation-response-drafts/route.ts","utf8");const panel=readFileSync("components/conversation-response-approval-panel.tsx","utf8");const backup=readFileSync("lib/backup-manifest.ts","utf8");
describe("conversation response draft approval boundary",()=>{it("stores one thread-linked response approval record per recommendation",()=>{expect(migration).toContain("recommendation_id uuid not null unique");expect(migration).toContain("reply_id uuid not null");expect(migration).toContain("status text not null default 'awaiting_approval'");});it("requires accepted recommendation before submission",()=>{expect(route).toContain('rec.status!=="accepted"');expect(route).toContain("SUBMIT_ACCEPTED_RESPONSE_FOR_APPROVAL");expect(panel).toContain("Submit for approval");});it("requires administrator authority to approve or reject",()=>{expect(route).toContain("requireAdmin()");expect(route).toContain('z.enum(["approved","rejected"])');expect(route).toContain("REVIEW_CONVERSATION_RESPONSE_DRAFT");});it("keeps content approval separate from delivery",()=>{expect(route).toContain("deliveryEnabled:false");expect(route).not.toContain("sendApprovedEmailWithResend");expect(route).not.toContain('/api/delivery-provider');expect(panel).toContain("Approval does not send the message");expect(panel).toContain("Approved · not sent");});it("backs up response approval evidence",()=>{expect(backup).toContain('"conversation_response_drafts"');});});
