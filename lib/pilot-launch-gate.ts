const requiredVerificationKeys=["invitation","approval_flow","backup_restore"] as const;export const PILOT_SMOKE_MAX_AGE_MS=24*60*60*1000;
export type PilotLaunchEvidence={latestSmokeStatus:string|null;latestSmokeCheckedAt:string|null;verificationKeys:string[]};
export function evaluatePilotLaunchGate(evidence:PilotLaunchEvidence,now=Date.now()){const missing=requiredVerificationKeys.filter(key=>!evidence.verificationKeys.includes(key));const checkedAt=evidence.latestSmokeCheckedAt?Date.parse(evidence.latestSmokeCheckedAt):NaN;const smokeFresh=Number.isFinite(checkedAt)&&checkedAt<=now&&now-checkedAt<=PILOT_SMOKE_MAX_AGE_MS;const smokeReady=evidence.latestSmokeStatus==="ready"&&smokeFresh;return{allowed:smokeReady&&missing.length===0,smokeReady,smokeFresh,missingVerifications:missing,reason:evidence.latestSmokeStatus!=="ready"?"Record a production smoke test with zero blockers before starting the pilot.":!smokeFresh?"Record a fresh production smoke test within 24 hours before starting the pilot.":missing.length?`Complete launch verification: ${missing.join(", ")}.`:"Pilot launch gates are satisfied."};}
export function statusStartsPilot(status:string){return status==="invited"||status==="active";}
