import type{OperatingTarget}from"./sales-operating-targets";
export type TargetHistoryRow=OperatingTarget&{previousValue:number|null;delta:number|null;isActive:boolean};
export function buildOperatingTargetHistory(targets:OperatingTarget[]){const groups=new Map<string,OperatingTarget[]>();for(const target of targets){const key=`${target.period}:${target.metric}`;groups.set(key,[...(groups.get(key)??[]),target])}const rows:TargetHistoryRow[]=[];for(const items of groups.values()){const ordered=[...items].sort((a,b)=>a.setAt.localeCompare(b.setAt));ordered.forEach((item,index)=>{const previous=index?ordered[index-1].targetValue:null;rows.push({...item,previousValue:previous,delta:previous===null?null:Math.round((item.targetValue-previous)*100)/100,isActive:item.effectiveTo===null})})}return rows.sort((a,b)=>b.setAt.localeCompare(a.setAt));}
