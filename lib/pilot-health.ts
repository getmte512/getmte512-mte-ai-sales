export type PilotHealthGate={allowed:boolean;reason:string;smokeReady:boolean;smokeFresh?:boolean;missingVerifications:string[]};
export type PilotHealthAccount={id:string;contactId:string;status:string};
export function summarizePilotHealth(accounts:PilotHealthAccount[],gate:PilotHealthGate){const live=accounts.filter(account=>account.status==="invited"||account.status==="active");const active=accounts.filter(account=>account.status==="active");const invited=accounts.filter(account=>account.status==="invited");const requiresReview=live.length>0&&!gate.allowed;return{liveCount:live.length,activeCount:active.length,invitedCount:invited.length,requiresReview,affectedAccountIds:requiresReview?live.map(account=>account.id):[],status:requiresReview?"degraded":live.length?"healthy":"idle",detail:requiresReview?`Pilot readiness degraded: ${gate.reason}`:live.length?"Controlled pilot accounts are live and current launch gates remain satisfied.":"No invited or active pilot accounts."} as const;}
