import{backupTables}from"./backup-manifest";
export function validateBackup(value:unknown){const errors:string[]=[];if(!value||typeof value!=="object")return{valid:false,errors:["Backup must be a JSON object."],counts:{}};const backup=value as Record<string,unknown>;if(backup.version!==1)errors.push("Unsupported backup version.");if(typeof backup.exportedAt!=="string"||Number.isNaN(Date.parse(backup.exportedAt)))errors.push("Export timestamp is missing or invalid.");const data=backup.data;if(!data||typeof data!=="object")errors.push("Backup data is missing.");const counts:Record<string,number>={};for(const table of backupTables){const rows=(data as Record<string,unknown>|undefined)?.[table];if(!Array.isArray(rows))errors.push(`${table} records are missing.`);else counts[table]=rows.length;}for(const forbidden of["auth","users","tokens","secrets"]){if(data&&Object.keys(data as object).some(key=>key.toLowerCase().includes(forbidden)))errors.push("Backup contains a forbidden credential collection.");}return{valid:errors.length===0,errors,counts};}
