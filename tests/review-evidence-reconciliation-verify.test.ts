import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{buildEvidenceReconciliationReport,verifyEvidenceReconciliationReport}from"../lib/review-evidence-reconciliation";
const route=readFileSync("app/api/review/evidence-reconciliation-verify/route.ts","utf8");const page=readFileSync("app/review/evidence-verify/page.tsx","utf8");
describe("reconciliation report verification",()=>{it("validates an untampered report and rejects changed comparison data",()=>{const report=buildEvidenceReconciliationReport({packageEvidenceHash:"a".repeat(64),comparison:{valid:false,missingSnapshots:["s1"]},generatedAt:"2026-08-18T01:00:00Z"});expect(verifyEvidenceReconciliationReport(report).valid).toBe(true);expect(verifyEvidenceReconciliationReport({...report,comparison:{valid:true}}).valid).toBe(false)});it("requires sales authorization and never queries or mutates the database",()=>{expect(route).toContain("requireSales");expect(route).toContain("verifyEvidenceReconciliationReport");expect(route).not.toMatch(/createAdminClient|\.from\(|\.insert\(|\.update\(|\.delete\(|\.rpc\(/)});it("surfaces reconciliation verification beside archived evidence verification",()=>{expect(page).toContain("ReviewEvidenceVerifier");expect(page).toContain("ReviewReconciliationVerifier")})});
