import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{crossCheckReviewEvidence}from"../lib/review-evidence-crosscheck";
const route=readFileSync("app/api/review/evidence-crosscheck/route.ts","utf8");
describe("review evidence current-state cross-check",()=>{it("reports exact missing and mismatched archived records",()=>{const result=crossCheckReviewEvidence({packageSnapshots:[{id:"s1",payload_hash:"a",target_hash:"b"},{id:"s2",payload_hash:"c",target_hash:"d"}],packageAnnotations:[{id:"a1",snapshot_id:"s1",annotation_type:"risk",body:"Original risk",created_by:"u",created_at:"t"},{id:"a2",snapshot_id:"s2",annotation_type:"decision",body:"Original decision",created_by:"u",created_at:"t2"}],currentSnapshots:[{id:"s1",payload_hash:"a",target_hash:"changed"}],currentAnnotations:[{id:"a1",snapshot_id:"s1",annotation_type:"risk",body:"Changed risk",created_by:"u",created_at:"t"}]});expect(result.valid).toBe(false);expect(result.missingSnapshots).toEqual(["s2"]);expect(result.mismatchedSnapshots).toEqual(["s1"]);expect(result.missingAnnotations).toEqual(["a2"]);expect(result.mismatchedAnnotations).toEqual(["a1"])});it("requires package verification before read-only database comparison",()=>{expect(route).toContain("requireSales");expect(route).toContain("verifyReviewEvidenceExport");expect(route).toContain("must pass integrity verification");expect(route).toContain('from("sales_operating_review_snapshots")');expect(route).toContain('from("sales_operating_review_annotations")');expect(route).not.toMatch(/\.insert\(|\.update\(|\.delete\(|\.rpc\(/)})});
