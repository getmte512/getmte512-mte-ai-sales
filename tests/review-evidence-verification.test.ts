import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{buildReviewEvidenceExport,verifyReviewEvidenceExport}from"../lib/review-evidence-export";
const route=readFileSync("app/api/review/evidence-verify/route.ts","utf8");
describe("review evidence verification",()=>{it("accepts an unchanged evidence package",()=>{const pack=buildReviewEvidenceExport({snapshots:[{id:"s",payload_hash:"a".repeat(64)}],annotations:[{id:"a",snapshot_id:"s",body:"Recorded context remains unchanged."}],generatedAt:"2026-08-18T01:00:00Z"});expect(verifyReviewEvidenceExport(pack).valid).toBe(true)});it("rejects tampered evidence and count mismatches",()=>{const pack=buildReviewEvidenceExport({snapshots:[{id:"s"}],annotations:[{id:"a",snapshot_id:"s",body:"Original context."}],generatedAt:"2026-08-18T01:00:00Z"});const tampered={...pack,annotations:[{id:"a",snapshot_id:"s",body:"Changed context."}]};expect(verifyReviewEvidenceExport(tampered).valid).toBe(false);const countMismatch={...pack,manifest:{...pack.manifest,annotationCount:2}};expect(verifyReviewEvidenceExport(countMismatch).valid).toBe(false)});it("keeps verification authenticated, bounded, and database-free",()=>{expect(route).toContain("requireSales");expect(route).toContain("5*1024*1024");expect(route).not.toContain("createAdminClient");expect(route).not.toMatch(/\.from\(|\.rpc\(|\.insert\(|\.update\(|\.delete\(/)})});
