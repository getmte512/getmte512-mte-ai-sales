import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";
const smoke=readFileSync("app/api/smoke-test/route.ts","utf8");const deployment=readFileSync("DEPLOYMENT.md","utf8");const roadmap=readFileSync("PROJECT_ROADMAP.md","utf8");
describe("milestone 34 schema and roadmap alignment",()=>{it("requires the annotation schema in production smoke",()=>{expect(smoke).toContain('"sales_operating_review_annotations"');expect(smoke).toContain('name:"Milestone 25 schema"');expect(smoke).toContain("migration 055")});it("documents the current migration ceiling",()=>{expect(deployment).toContain("055_operating_review_annotations.sql");expect(deployment).toContain("Milestone 25 schema")});it("keeps the roadmap aligned through the current operating-review layers",()=>{expect(roadmap).toContain("Milestone 25");expect(roadmap).toContain("Milestone 33");expect(roadmap).toContain("Milestone 34")})});
