import{describe,expect,it}from"vitest";import{buildReviewEvidenceExport}from"../lib/review-evidence-export";import{readFileSync}from"node:fs";
const route=readFileSync("app/api/review/evidence-export/route.ts","utf8");
describe("review evidence export",()=>{it("hashes immutable evidence independently of export time",()=>{const input={snapshots:[{id:"s",payload_hash:"abc"}],annotations:[{id:"a",snapshot_id:"s",body:"Recorded context."}]};const one=buildReviewEvidenceExport({...input,generatedAt:"2026-08-18T01:00:00Z"});const two=buildReviewEvidenceExport({...input,generatedAt:"2026-08-18T02:00:00Z"});expect(one.manifest.evidenceHash).toBe(two.manifest.evidenceHash);expect(one.manifest.snapshotCount).toBe(1);expect(one.manifest.annotationCount).toBe(1)});it("requires sales authorization and performs read-only evidence queries",()=>{expect(route).toContain("requireSales");expect(route).toContain('from("sales_operating_review_snapshots")');expect(route).toContain('from("sales_operating_review_annotations")');expect(route).not.toMatch(/\.insert\(|\.update\(|\.delete\(|\.rpc\(/);expect(route).toContain("content-disposition")})});
