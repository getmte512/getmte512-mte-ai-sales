import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";
const migration=readFileSync("supabase/migrations/055_operating_review_annotations.sql","utf8");
const route=readFileSync("app/api/review/annotations/route.ts","utf8");
const backup=readFileSync("lib/backup-manifest.ts","utf8");
describe("milestone 25 review annotations",()=>{it("keeps annotations append-only and snapshot-bound",()=>{expect(migration).toContain("snapshot_id uuid not null references public.sales_operating_review_snapshots(id) on delete restrict");expect(migration).toContain("grant select on public.sales_operating_review_annotations to service_role");expect(migration).not.toMatch(/grant\s+(insert|update|delete).*sales_operating_review_annotations\s+to\s+authenticated/i)});it("requires sales access, explicit confirmation, and the audited RPC",()=>{expect(route).toContain("requireSales");expect(route).toContain('z.literal("ADD_REVIEW_ANNOTATION")');expect(route).toContain('rpc("add_sales_operating_review_annotation"');expect(migration).toContain("sales_operating_review_annotation_added")});it("includes annotation evidence in recovery coverage",()=>{expect(backup).toContain('"sales_operating_review_annotations"')})});
