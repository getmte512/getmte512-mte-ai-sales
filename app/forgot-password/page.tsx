import Link from"next/link";import{requestPasswordReset}from"./actions";
export default async function ForgotPassword({searchParams}:{searchParams:Promise<{sent?:string}>}){const params=await searchParams;return <main className="login-shell"><form action={requestPasswordReset} className="panel login-card"><span className="eyebrow dark">MORE THAN ENERGY</span><h1>Reset password</h1><p>Enter your authorized MTE account email.</p>{params.sent&&<div className="alert success">If the account exists, a secure password-reset email has been sent.</div>}<label>Email<input name="email" type="email" autoComplete="email" required/></label><button className="primary">Send reset link</button><Link className="login-link" href="/login">Return to sign in</Link></form></main>}
