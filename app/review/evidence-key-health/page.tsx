import Link from"next/link";import{redirect}from"next/navigation";import{requireAdmin}from"@/lib/authorization";import{getReviewEvidenceKeyHealth}from"@/lib/review-evidence-signing";
export default async function EvidenceKeyHealthPage(){try{await requireAdmin()}catch(error){if(error instanceof Error&&error.message==="UNAUTHORIZED")redirect("/login");throw error}const health=getReviewEvidenceKeyHealth();return <main><header className="topbar"><div><span className="eyebrow">MORE THAN ENERGY</span><h1>Evidence Signing Key Health</h1></div><div className="account-controls"><Link className="sign-out-button" href="/review/evidence-verify">Evidence Verification</Link><Link className="sign-out-button" href="/review">Operating Review</Link><Link className="sign-out-button" href="/">Back to CRM</Link></div></header><section className="workspace"><section className="panel"><span className="eyebrow">ADMIN · READ ONLY</span><h2>{health.healthy?"Signing-key configuration is healthy":"Signing-key configuration needs attention"}</h2><p>This view exposes lifecycle metadata and verification-material presence only. Signing secrets and retained secret values are never rendered.</p><div className="metric-grid"><article className="metric-card"><span>Lifecycle registry</span><strong>{health.lifecycleConfigured?"Configured":"Legacy mode"}</strong></article><article className="metric-card"><span>Active key ID</span><strong>{health.activeKeyId??"Invalid"}</strong></article><article className="metric-card"><span>Retained keys</span><strong>{health.retainedKeyIds.length}</strong></article><article className="metric-card"><span>Compromised keys</span><strong>{health.compromisedKeyIds.length}</strong></article></div>{health.error?<p className="error-message">{health.error}</p>:null}</section>{health.keys.length?<section className="panel"><span className="eyebrow">LIFECYCLE REGISTRY</span><h2>Configured key eras</h2><div className="table-wrap"><table><thead><tr><th>Key ID</th><th>Status</th><th>Activated</th><th>Retired</th><th>Change ref</th><th>Incident ref</th><th>Retained material</th></tr></thead><tbody>{health.keys.map(key=><tr key={key.keyId}><td><code>{key.keyId}</code></td><td>{key.status}</td><td>{key.activatedAt}</td><td>{key.retiredAt??"—"}</td><td>{key.changeRef}</td><td>{key.incidentRef??"—"}</td><td>{key.status==="active"?"Active secret":""+(key.retainedVerificationMaterial?"Present":"Missing")}</td></tr>)}</tbody></table></div></section>:<section className="panel"><span className="eyebrow">LIFECYCLE REGISTRY</span><h2>No machine lifecycle registry configured</h2><p>The application is using the compatibility path. Configure the non-secret lifecycle registry before relying on machine-visible key-era health.</p></section>}</section></main>}
