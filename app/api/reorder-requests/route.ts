import{NextResponse}from"next/server";import{requireSales}from"@/lib/authorization";import{createAdminClient}from"@/lib/supabase/admin";import{calculateReorderRequestTotal,reorderDecisionSchema,reorderRequestSchema}from"@/lib/reorder-request";
export async function GET(){try{await requireSales();const supabase=createAdminClient();const{data,error}=await supabase.from("reorder_requests").select("id,contact_id,status,currency_code,estimated_total,lines,customer_note,created_at,contacts(company_name,buyer_name)").order("created_at",{ascending:false}).limit(100);if(error)throw error;return NextResponse.json({requests:data??[]});}catch(error){const message=error instanceof Error?error.message:"Unable to load reorder requests.";return NextResponse.json({error:message==="UNAUTHORIZED"?"Sign in is required.":message==="FORBIDDEN"?"Assigned team access is required.":message},{status:message==="UNAUTHORIZED"?401:message==="FORBIDDEN"?403:500});}}
export async function POST(request:Request){try{const{user}=await requireSales();const body=reorderRequestSchema.parse(await request.json());const estimatedTotal=calculateReorderRequestTotal(body.lines);const supabase=createAdminClient();const{data,error}=await supabase.from("reorder_requests").insert({contact_id:body.contactId,status:"pending_review",currency_code:body.currencyCode,estimated_total:estimatedTotal,lines:body.lines,customer_note:body.customerNote||null,requested_by:user.id}).select("id,status,estimated_total").single();if(error)throw error;return NextResponse.json({request:data,shopifyOrderCreated:false});}catch(error){const message=error instanceof Error?error.message:"Unable to save reorder request.";return NextResponse.json({error:message==="UNAUTHORIZED"?"Sign in is required.":message==="FORBIDDEN"?"Assigned team access is required.":message},{status:message==="UNAUTHORIZED"?401:message==="FORBIDDEN"?403:400});}}
export async function PATCH(request:Request){try{const{user}=await requireSales();const body=reorderDecisionSchema.parse(await request.json());const supabase=createAdminClient();const{data,error}=await supabase.from("reorder_requests").update({status:body.decision,reviewed_by:user.id,reviewed_at:new Date().toISOString(),updated_at:new Date().toISOString()}).eq("id",body.id).eq("status","pending_review").select("id,status").single();if(error)throw error;return NextResponse.json({request:data,shopifyOrderCreated:false});}catch(error){const message=error instanceof Error?error.message:"Unable to review reorder request.";return NextResponse.json({error:message==="UNAUTHORIZED"?"Sign in is required.":message==="FORBIDDEN"?"Assigned team access is required.":message},{status:message==="UNAUTHORIZED"?401:message==="FORBIDDEN"?403:400});}}
