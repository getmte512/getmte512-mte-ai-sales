import{NextResponse}from"next/server";import{requireUser}from"@/lib/supabase/server";import{createAdminClient}from"@/lib/supabase/admin";
export async function GET(){try{await requireUser();const supabase=createAdminClient();const[{data:runs,error:runError},{count,error:linkError}]=await Promise.all([supabase.from("shopify_sync_runs").select("id,sync_type,status,reviewed_count,imported_count,exception_count,created_at").order("created_at",{ascending:false}).limit(50),supabase.from("shopify_customer_links").select("id",{count:"exact",head:true})]);if(runError)throw runError;if(linkError)throw linkError;return NextResponse.json({runs:runs??[],approvedLinks:count??0});}catch(error){const message=error instanceof Error?error.message:"Unable to load Shopify sync history.";return NextResponse.json({error:message==="UNAUTHORIZED"?"Sign in is required.":message},{status:message==="UNAUTHORIZED"?401:500});}}
