import{NextResponse}from"next/server";import{z}from"zod";import{requireSales}from"@/lib/authorization";import{createAdminClient}from"@/lib/supabase/admin";
const schema=z.object({recommendation_id:z.string().uuid(),confirmation:z.literal("CREATE_RECOMMENDED_FOLLOW_UP_TASK")});
export async function POST(request:Request){try{const{user}=await requireSales();const body=schema.parse(await request.json());const supabase=createAdminClient();const{data,error}=await supabase.rpc("apply_conversation_recommendation_task",{p_recommendation_id:body.recommendation_id,p_actor_id:user.id});if(error)throw error;return NextResponse.json({result:data,message:(data as{already_applied?:boolean}|null)?.already_applied?"This accepted recommendation already has a follow-up task; no duplicate task was created.":"Follow-up task created from the accepted recommendation. No message was sent."});}catch(error){const message=error instanceof Error?error.message:"Unable to create follow-up task.";return NextResponse.json({error:message==="UNAUTHORIZED"?"Sign in is required.":message==="FORBIDDEN"?"Sales access is required.":message},{status:message==="UNAUTHORIZED"?401:message==="FORBIDDEN"?403:400});}}
