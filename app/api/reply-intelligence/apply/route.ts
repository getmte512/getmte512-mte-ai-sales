import{NextResponse}from"next/server";import{z}from"zod";import{requireSales}from"@/lib/authorization";import{createAdminClient}from"@/lib/supabase/admin";
const schema=z.object({recommendation_id:z.string().uuid(),confirmation:z.literal("APPLY_ACCEPTED_RECOMMENDATION_TO_PIPELINE")});
export async function POST(request:Request){try{const{user}=await requireSales();const body=schema.parse(await request.json());const supabase=createAdminClient();const{data,error}=await supabase.rpc("apply_conversation_recommendation_to_pipeline",{p_recommendation_id:body.recommendation_id,p_actor_id:user.id});if(error)throw error;return NextResponse.json({result:data,message:(data as{already_applied?:boolean}|null)?.already_applied?"This accepted recommendation was already applied; no duplicate pipeline mutation occurred.":"Accepted recommendation applied to the CRM pipeline. No message was sent."});}catch(error){const message=error instanceof Error?error.message:"Unable to apply recommendation.";return NextResponse.json({error:message==="UNAUTHORIZED"?"Sign in is required.":message==="FORBIDDEN"?"Sales access is required.":message},{status:message==="UNAUTHORIZED"?401:message==="FORBIDDEN"?403:400});}}
