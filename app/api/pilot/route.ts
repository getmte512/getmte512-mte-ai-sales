import{NextResponse}from"next/server";import{requireAdmin}from"@/lib/authorization";import{createAdminClient}from"@/lib/supabase/admin";import{addPilotSchema,updatePilotSchema}from"@/lib/pilot-account";import{evaluatePilotLaunchGate,statusStartsPilot}from"@/lib/pilot-launch-gate";

async function getPilotLaunchGate(){const supabase=createAdminClient();const[{data:smoke,error:smokeError},{data:verifications,error:verificationError},{data:drill,error:drillError}]=await Promise.all([supabase.from("launch_smoke_runs").select("status,checked_at").order("checked_at",{ascending:false}).limit(1).maybeSingle(),supabase.from("launch_verifications").select("verification_key"),supabase.from("backup_recovery_drills").select("status,checked_at").order("checked_at",{ascending:false}).limit(1).maybeSingle()]);if(smokeError)throw smokeError;if(verificationError)throw verificationError;if(drillError)throw drillError;return evaluatePilotLaunchGate({latestSmokeStatus:smoke?.status??null,latestSmokeCheckedAt:smoke?.checked_at??null,verificationKeys:(verifications??[]).map(row=>row.verification_key),latestRecoveryDrillStatus:drill?.status??null,latestRecoveryDrillCheckedAt:drill?.checked_at??null});}

export async function GET(){try{await requireAdmin();const supabase=createAdminClient();const[{data,error},gate]=await Promise.all([supabase.from("pilot_accounts").select("id,contact_id,status,feedback_notes,created_at,updated_at").order("updated_at",{ascending:false}),getPilotLaunchGate()]);if(error)throw error;return NextResponse.json({accounts:data??[],launchGate:gate});}catch(error){const message=error instanceof Error?error.message:"Unable to load pilot accounts.";return NextResponse.json({error:message==="UNAUTHORIZED"?"Sign in is required.":message==="FORBIDDEN"?"Administrator access is required.":message},{status:message==="UNAUTHORIZED"?401:message==="FORBIDDEN"?403:500});}}
export async function POST(request:Request){try{const{user}=await requireAdmin();const body=addPilotSchema.parse(await request.json());const supabase=createAdminClient();const{data,error}=await supabase.from("pilot_accounts").upsert({contact_id:body.contactId,status:"selected",added_by:user.id,updated_by:user.id,updated_at:new Date().toISOString()},{onConflict:"contact_id"}).select("*").single();if(error)throw error;return NextResponse.json({account:data});}catch(error){const message=error instanceof Error?error.message:"Unable to add pilot account.";return NextResponse.json({error:message==="UNAUTHORIZED"?"Sign in is required.":message==="FORBIDDEN"?"Administrator access is required.":message},{status:message==="UNAUTHORIZED"?401:message==="FORBIDDEN"?403:400});}}
export async function PATCH(request:Request){try{const{user}=await requireAdmin();const body=updatePilotSchema.parse(await request.json());if(statusStartsPilot(body.status)){const gate=await getPilotLaunchGate();if(!gate.allowed)return NextResponse.json({error:gate.reason,launchGate:gate},{status:409});}const supabase=createAdminClient();const{data,error}=await supabase.rpc("update_pilot_account_status",{p_actor_id:user.id,p_id:body.id,p_status:body.status,p_feedback_notes:body.feedbackNotes||null});if(error)throw error;return NextResponse.json({account:data});}catch(error){const message=error instanceof Error?error.message:"Unable to update pilot account.";return NextResponse.json({error:message==="UNAUTHORIZED"?"Sign in is required.":message==="FORBIDDEN"?"Administrator access is required.":message},{status:message==="UNAUTHORIZED"?401:message==="FORBIDDEN"?403:400});}}
